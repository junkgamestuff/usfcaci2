<nav class="mc--menu mc--{{ menu.name }}" {% if menu.ariaLabel is not empty %}aria-label="{{ menu.ariaLabel|default('Navigation') }}"{% endif %}>
  <ul class="m--menu m--{{ menu.name }}">
    {% block content %}

      {% for item in menu.items %}

        {% include "base-menu-item-alt" with { item: item } %}

      {% endfor %}

    {% endblock %}
  </ul><!-- m--menu -->
</nav>
