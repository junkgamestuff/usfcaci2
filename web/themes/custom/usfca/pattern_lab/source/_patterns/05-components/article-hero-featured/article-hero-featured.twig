{% extends "base-component" %}

{% set name = "article-hero-featured" %}
{% set container_element = 'header' %}

{% block content %}

  <div class="image-wrapper">
    {% include "fields-image" with {
      image: bannerImage
    } only %}
  </div>

  <div class="text-wrapper">
    {% if category is not empty %}
      {% include "fields-category" with { category: category } only %}
    {% endif %}

    {% include "fields-page-title" with { title: title } only %}

    {% if description is not empty %}
      {% include "fields-description" with { description: description } only %}
    {% endif %}

    {% include "fields-authored-date" with {
      fullname: fullname,
      byline: byline,
      date: date
    } only %}

  </div>

{% endblock %}
